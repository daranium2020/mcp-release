name: Test Action v0.1.1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Smoke-test daranium2020/mcp-release@v0.1.1
    runs-on: ubuntu-latest

    steps:
      - name: Run MCP Release check
        uses: daranium2020/mcp-release@v0.1.1
        with:
          endpoint: https://mcp-release-fixture.vercel.app/mcp
