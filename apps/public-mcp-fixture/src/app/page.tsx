export default function Page() {
  return (
    <main>
      <h1>MCP Fixture Server</h1>
      <p>
        <code>POST /mcp</code> — MCP Streamable HTTP endpoint
      </p>
      <p>
        <code>GET /health</code> — Health check
      </p>
    </main>
  );
}
