{
  "name": "@mcp-launch/github-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action for MCP server release validation",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@mcp-launch/core": "workspace:*",
    "@mcp-launch/reporter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
