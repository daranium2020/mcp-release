{
  "name": "@mcp-release/github-action",
  "version": "0.1.2",
  "private": true,
  "description": "GitHub Action for MCP server release validation",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@mcp-release/core": "workspace:*",
    "@mcp-release/reporter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
